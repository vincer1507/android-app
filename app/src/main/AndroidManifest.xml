<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fyp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RatingActivtiy"></activity>
        <activity android:name=".MaterialYoutubeVideoActivity" />
        <activity android:name=".MaterialArticleActivity" />
        <activity android:name=".MaterialYoutubeActivity" />
        <activity
            android:name=".MaterialActivity"
            android:label="@string/title_activity_material"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".chatroomMainActivity" />
        <activity android:name=".TimeUpActivity" />
        <activity android:name=".GameWonActivity" />
        <activity android:name=".PlayAgainActivity" />
        <activity android:name=".QuizGameStartPageActivity" />
        <activity android:name=".QuizGamePageActivity" />
        <activity android:name=".DiaryActivity" />
        <activity android:name=".PreviewActivity" />
        <activity android:name=".SpeechActivity" />
        <activity android:name=".TimeLineActivity" />
        <activity android:name=".MaterialArticleDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.here.this.library.provider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities" />
    </application>

</manifest>